# L05 操作手册命令复验说明

复验日期：2026-09-09。性质：编写者在隔离目录执行的命令验证，不是个人学习达成、同伴复验或人工接受。

现行手册的准备、两次失败、检查冻结、真实 Codex 修复、原命令复验和任务读取已走通。Windows PowerShell 命令块共 22 个，包含服务终端、续做和故障排查分支；不能把全部代码块无差别一次执行。

## 已核对的结果

| 环节 | 实际结果 |
|---|---|
| 课堂入库副本 | 旧检查退出 0，完整检查连续两次退出 1，均定位重复流水 |
| 工程范围练习 | 合法修改通过；越界新增连续失败；重命名越界失败；恢复后通过 |
| 正式缺陷候选 | 业务检查连续两次失败，工程检查通过，默认入库检查失败 |
| 检查版本 | 三份检查进入会话提交，冻结指纹按 CRLF→LF 规范化比较；实际代码内容未变 |
| 真实修复 | TASK-CC59509BA9；真实 Codex CLI 执行；仅改变 flowerp/service.py |
| 最终业务与工程 | 业务 4/4、工程 2/2；换为期初 11、收货 3 后业务仍为 4/4 |
| 最终阻断套件 | 隔离课程候选 20/20；控制仓库套件另为 29/29，二者版本不同 |
| 服务与续做 | 保存的运行目录可启动首页，HTTP 200；会话恢复与 22 个 PowerShell 块语法检查通过 |
| 人工决定 | review，reviewed_by 为空；未人工接受、未集成或发布 |

本轮累计保存 46 份不可覆盖的命令回执，其中 43 份退出码符合该步骤预期。预期退出 1 的红灯仍保留为失败证据，不写成业务成功。

## 排查中保留的失败

- TASK-DBF8E78981：工具宿主连接超时，没有源码变化。
- TASK-1ECD458A90：禁用 code_mode_host 后工具不可用；已撤回无效做法。
- TASK-F033B86CC1：默认课程检查通过，但独立文件级复验发现额外修改 eval/cases.py，因此不作为完成结果。
- 独立 CLI 最初未保留 Windows 沙箱设置，只读命令被策略拒绝；保留现有 Windows 沙箱设置后只读命令实际执行。
- 本次成功执行使用手册 6.1 的可选 isolated 配置模式，保留模型与 Windows 沙箱，不修改全局配置。新增 --allowed-file 将文件级授权传入实际任务，拒绝扩大课程合同。

## 原始证据

- [本次会话](../../../.runtime/l05-practice/20260909-140218-6c6b4efb/session.json)
- [命令回执目录](../../../.runtime/l05-practice/20260909-140218-6c6b4efb/evidence)
- [最终任务返回](../../../.runtime/l05-practice/20260909-140218-6c6b4efb/submission.json)
- [最终执行记录](../../../.runtime/l05-practice/20260909-140218-6c6b4efb/verification-runtime/delivery/TASK-CC59509BA9/evidence.json)
- [最终阻断报告](../../../.runtime/l05-practice/20260909-140218-6c6b4efb/blocking-final.json)
- 服务启动验证（本机历史报告：`../../../.course-authoring/L05/executable-manual/server-smoke.json`，不随 Git 发布）
- 控制仓库阻断报告（本机历史报告：`../../../.course-authoring/L05/executable-manual/controller-blocking.json`，不随 Git 发布）

相关自动化测试覆盖：重复失败不覆盖证据、退出码不符停止、冻结记录拒绝覆盖、检查篡改与执行后变更拒绝、范围只能收窄、独立配置保留模型与沙箱、课程红绿差分和执行流超时。测试夹具不替代上述真实 CLI 执行。

## 使用边界

连续命令以本机已安装的项目虚拟环境和可运行 Codex CLI 为前提。个人插件、登录和自定义模型提供方可能影响子进程；失败后按手册保留回执并续做，不删证据或放宽授权。教学支架验证的是给定用例，仍需你自行补充和解释一条断言，由真实复验者作独立判断。

Word 版已由现行 Markdown 重建，并经 Word 导出为 12 页进行版面检查。Markdown 是复制命令的首选来源，Word 适合阅读。
