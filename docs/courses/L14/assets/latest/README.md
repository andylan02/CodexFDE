# L14 原生浏览器观察

四张图片直接截取本地工作台的原生页面区域，没有重绘界面或替换文字。任务为 TASK-0456F5029A，模式 verify；主动让浏览器离线后恢复，服务持续运行。

- 08-stage-focus：自动检查结束，任务仍待审核，Codex 未调用。
- 09-eval-focus：展开本次三个实际检查。
- 10-offline-focus：读取失败后停止显示旧结论和审核动作。
- 11-restored-focus：恢复后仍是同一任务和 review 状态。

browser-evidence.json 保存观察字段及边界。无实际 Codex 开发、采购单变更、正向补丁下载或具名验收。本次课程任务未与首页事项关联。


## 客户采购与下载实测

erp-01-pending、erp-02-maker-refused、erp-03-purchase-offline、erp-04-received 来自另一隔离客户账套的原生页面。制单账号原生点击审批并确认后返回409；后续另一教学账号审批、收货、同身份重放由脚本调用真实 API。库存为3/0/3，目标入库流水为1；inventory.csv 为实际浏览器下载，erp-evidence.json 保存对象、响应、只读查询与文件摘要。

采购离线图保留实际缺口：旧状态和数据已同步提示未明确失效。它不是修复后截图。两个教学账号不等于真人审批；业务 CSV 不等于研发 Task 的代码候选。上面的工作台 Task 与此采购数据没有自动关联。
