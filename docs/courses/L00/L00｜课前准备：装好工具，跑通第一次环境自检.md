# L00｜课前准备：装好工具，跑通第一次环境自检

## 欢迎：我们先从一件你想做成的小事开始

欢迎来到 Codex AI 工程交付行动营。

也许你试过让 AI 帮忙写程序，但遇到报错后不知道该怎么办。这门课从这样的起点出发：你有一个需要解决的问题，Codex 可以帮你调查、实现和运行，但你需要逐渐学会判断它做的事情是否符合要求。

想象一下，你希望每天把订单整理成一份清单。你告诉 AI “帮我做一个导出功能”，它很快回复“已经完成”。这时还有几个实际问题：导出的是什么数据？空订单会怎样？文件保存在哪里？下次还能运行吗？它有没有顺手改动其他东西？我们会在课程里把这些问题变成可以检查的步骤。

**你将先用 Codex 搭建个人 AI 研发工作台，再通过这个工作台持续开发电商客户产品 FlowERP。** 最后带走的成果包括能继续使用的工作台、逐步增长的 FlowERP，以及你自己组织交付和作出判断的记录。

这份文档是开课前给你独立阅读、动手准备的预习资料。今天先认识学习路线、装好工具、完成几个小练习。工作台的建设从 L01 开始，FlowERP 的首次接入与交付在 L04。

### 这门课与你有什么关系

你不需要先成为程序员才能开始准备。你需要愿意把一个模糊想法说清楚，按步骤尝试，保存真实结果，并在不确定时提出问题。遇到代码，我们会先说明它完成什么、怎样观察结果，再解释当前需要理解的部分。

四周课程也不会自动让每个人掌握所有编程知识。随着任务推进，你会补学必要的文件、命令、代码和业务概念。你能否跟上，取决于动手与反馈，不能只用视频是否看完来判断。已经有开发经验的同学可以减少学习提示，但仍要完成判断和复验。

### 先分清三个名字

|名字|你可以怎样理解|课程里由谁来建设、使用|
|---|---|---|
|Codex|与你一起工作的 AI 开发伙伴，可以理解项目并使用工具|你提出目标、检查计划和结果，Codex 协助调查与实现|
|个人 AI 研发工作台|组织研发工作的地方，记录需求、任务、修改、检查和审核|你先与 Codex 一起搭建，随后用它组织交付|
|FlowERP|电商客户使用的软件，逐步管理进货、销售、库存和财务|你通过工作台与 Codex 协作开发，用真实业务结果检验工作台|

例如，同一张入库单收到两次，库存应只增加一次。以后我们会通过这样的业务问题学习怎样写清要求、检查错误、修复和签收。现在只需理解这个现象，不必提前学习数据库或财务知识。

### 四周里，你会看到什么变化

|阶段|你逐渐能做的事|看得见的成果|
|---|---|---|
|第一周，L01～L04|说清范围，让 Codex 在边界内工作，并保存检查记录|最小工作台；L04 第一次交付库存导出|
|第二周，L05～L08|用正常与失败例子检查结果，在本机与远端重复检查|幂等入库、库存口径、订单创建与原子预占逐步完善|
|第三周，L09～L12|安排返工与分工，知道何时停止、何时交给人审核|订单取消与状态修复、采购申请及审批后入库|
|第四周，L13～L16|使用界面、处理反馈，让他人也能启动和检查交付|可操作的工作台与 FlowERP，完成一个现场新需求|

这里的“幂等入库”就是同一笔入库重复提交仍只生效一次；“原子预占”先理解为一张订单要占的库存要么全部成功，要么不占，不能只占一半。其他术语会在相应课次用实例解释，无需提前背诵。

### 你和 Codex 分别负责什么

Codex 可以给出建议和实现，也可能误解你的要求。你不必在第一天就看懂每一行代码，但需要逐渐守住四个检查点：这次要做什么、允许改哪里、结果怎样检查、现在是否有足够证据接受。

当一条命令失败时，先记录实际发生了什么，再决定下一步。让 Codex 解释错误或提供一个提示，比反复说“再修一下”更有助于你理解问题。不要让它代写你没有做过的步骤，也不要把自己的不确定删掉。

### 每讲怎样学，怎样知道自己学会了

1. **课前看问题**：读一个具体场景，先写下你目前的判断；陌生词先看例子。

2. **课中分段看示范**：遇到暂停点，预测结果或解释一处变化，再继续看。

3. **课后动手**：在本讲指定工作区完成小任务，保存失败、修改和再次检查的结果。

4. **请别人复验**：让同学或老师依据记录定位同一件事，得到可比较的结论。

5. **自己答一道变化题**：暂时不让 AI 代答，说明为什么可以接受、为什么要退回，或还缺什么证据。

“程序运行成功”和“我能解释它为什么符合要求”分别检查。你不用脱离 Codex 重写整个系统，但需要能用自己的话解释关键决定。

线上每讲约 30 分钟，完整动手会花更长时间。先预留一段不被打断的练习时间，卡住时记录进度，分次完成。

## 这份预习资料怎样使用

**第一次来：读完前面的****欢迎****导读及下方基本名词后，按****文档****顺序操作****。**

可以把全文分三次使用：**先读****欢迎导读****，认识课程；再按自己的操作系统安装；最后完成自检和提交。**后面的报错解释按需查阅，不必一次全部读完。

快速定位：安装工具 · 下载仓库 · 创建环境 · 环境自检 · 小练习 · 提交与准备状态 · 求助

|你现在的情况|从哪里读起|何时可以停下来|
|---|---|---|
|第一次接触编程工具|先读本页导读与“操作前认识五件事”，再按第一至八节准备|一次完成一个检查点，卡住后按求助模板记录|
|已会安装和运行 Python<br>|检查第一节版本口径；已有本仓库与 `.venv` 时做第七至九节，否则先做第四至六节|环境自检、小练习与只读核验都完成|
|工具安装遇到问题|找第十二节对应现象，填写求助信息|可以提交“环境受阻”，不用重装全部工具|

### 操作前认识五件事

|你将看到的东西|白话解释|现在只要会什么|
|---|---|---|
|文件与文件夹|文件保存内容，文件夹组织文件|找到一个指定文件并打开|
|路径|文件或文件夹的地址|认出当前所在目录；知道示例盘符可以不同|
|终端|输入命令、查看程序输出的窗口|一次输入一行，按回车，等结果出现|
|仓库|由 Git 管理、能跟踪修改的项目目录|在课程目录运行 `git status`|
|虚拟环境 `.venv`|课程专用的 Python 工具目录|用这个目录中的 Python 运行课程命令|

VS Code 用来读和编辑文件，也能打开终端；Codex 对话框用来提出自然语言任务。讲义里的命令放进终端，练习提示词放进 Codex 对话框。文件路径通常用于定位或打开文件，本身不一定是一条命令。

命令框上写 `powershell` 或 `bash` 表示适用的终端，不是需要复制的内容。一次复制框内一行，不要复制行前的终端提示符，也不要把说明文字一起粘进去。带有“你的姓名”等字样时，先换成自己的内容。

### 今天做到哪里，就可以进入下一步

|检查点|你亲手完成的动作|留下什么|
|---|---|---|
|A 工具能用|Python、Git、VS Code 与 Codex 能打开或显示版本|真实版本，不抄示例数字|
|B 项目能找到|打开课程根目录与 `.venv`，导入课程包|目录、解释器路径和 `imports-ok` 输出|
|C 结果能解释|完成四个小练习，人工核对两个文件|首次预测、实际结果和自己的解释|
|D 记录能交接|保存自检文档，说明准备状态|一份文件与一个待解问题|

开始安装前，先准备一份记录文件：有空白模板就先另存；暂时没有模板时，用记事本或纯文本编辑器建立 `L00-临时记录.txt`，记下步骤与实际输出。获取模板后再转填已有内容。

每到一个检查点就保存记录。需要帮助时，告诉老师你停在 A、B、C 还是 D，即使没做完也能得到针对性的支持。

**今天的边界**：可以安装、下载、查看文件、做小练习和写自检文档。完整演示、Web 服务、业务初始化、全量测试与 blocking Eval 留到相应课次。

## 一、先选择跟课环境

### 工具为什么需要安装

|工具|它在你电脑上做什么|你今天会用到的功能|
|---|---|---|
|Python|按代码里的步骤进行计算和执行|跑一个小算式、检查课程包能否加载|
|Git|记录项目文件及其变化|下载课程仓库、查看文件变化|
|VS Code|打开和编辑项目文件|看讲义、找文件、保存自检记录|
|Codex|根据你的要求协助理解和开发项目|只读回答问题，并指出文件证据|

这里的 “CLI” 就是从终端输入命令来使用工具；“解释器”就是实际执行 Python 代码的程序。现在不用记全部名称，操作时可回来看。

### 推荐主路径

|项目|课程建议|
|---|---|
|操作系统|满足所选工具官方要求的 64 位 Windows 或 macOS；安装前核对下载页的系统要求|
|编辑器|VS Code 当前稳定版|
|Python|**课程统一基线：CPython 3\.11\.x**；仓库声明最低为 Python 3\.10|
|Git|Git 2\.40\+；优先安装官网当前稳定版|
|Codex|Codex 桌面应用 \+ Codex CLI 当前稳定版；VS Code Codex 扩展可选|
|Shell|Windows 使用 PowerShell；macOS 使用 Terminal / zsh|
|浏览器|Chrome、Edge 或 Safari 当前稳定版|

课堂统一口径是 Windows 或 macOS。Linux 可以使用，但不作为课堂排错与演示的统一环境；自行使用 Linux 的同学，需要能独立把本讲命令对应到自己的发行版。

课程统一使用 Python 3\.11\.x，与当前参考仓库实际运行环境一致。本轮已核验的仓库 `.venv` 版本为 3\.11\.15；你需记录自己的完整版本，不必照抄补丁号。基础较弱的同学，不要在开课当天自行切换解释器版本，也不要按 README 里可能出现的 `py -3` 创建环境；请始终使用 `py -3.11` 或 `python3.11`。已有其他版本的同学，可以保留原环境，但本次预习中的验收仍使用 3\.11\.x；不要将仓库最低支持版本与课堂统一基线混用。

### 电脑配置

|项目|最低建议|更舒适的配置|
|---|---|---|
|内存|8 GB|16 GB 及以上|
|可用磁盘|10 GB|20 GB 及以上|
|网络|能稳定登录并使用 Codex，能访问课程仓库与官方文档|有线或稳定 Wi\-Fi|
|显卡|无要求|无要求|

本课程不在本地训练或部署大模型，不需要独立显卡，不要求安装 CUDA，也不要求购买 GPU 云主机。课程跟跑代码以 Python 标准库和 SQLite 为主，SQLite 随 Python 提供，不需要单独安装数据库服务器。

## 二、必须安装的工具

先打开终端：Windows 在开始菜单搜索并打开 “PowerShell”；macOS 按 Command \+ 空格，搜索 “Terminal／终端” 并打开。看到可输入文字的一行后再执行命令，不需要管理员模式。以后也可以使用 VS Code 的“终端 → 新建终端”。

`PATH` 是操作系统寻找命令的位置列表；安装器中的“ Add to PATH” 让你可以直接输入工具名称。`.venv` 则是项目里的 Python 环境，这两个概念不相同。

### 1\. Python 3\.11

只做自己系统的一条路线。如果已有 `Python 3.11.x` 且验证命令可运行，跳过安装。

**Windows：官方安装管理器路线**

- 打开 [Python 官方 Windows 使用说明](https://docs.python.org/3/using/windows.html)，从文档指向的 python\.org 或 Microsoft Store 安装 **Python Install Manager**。

- 关闭并重新打开 PowerShell，输入下面命令。`pymanager` 管理安装，`py -3.11` 明确选择课堂版本。

```PowerShell
pymanager install 3.11
py -3.11 --version
py -3.11 -m pip --version
```

第一行需要联网下载，请等它结束再继续。第二行应以 `Python 3.11` 开头，第三行应显示 pip 版本和位置。若 `pymanager` 找不到，先确认安装管理器已安装并重开终端；旧版 `py` 启动器本身不代表安装管理器已安装。不要卸载正在用于其他项目的 Python。

**macOS：官方图形安装器路线**

1. 打开 [Python 3\.11\.9 官方发布页](https://www.python.org/downloads/release/python-3119/)，在 Files 中选择 **macOS 64\-bit universal2 installer**，下载 `.pkg`。不要下载 Source tarball 源码包。

2. 打开 `.pkg`，按安装器提示完成；重新打开 Terminal。

3. 输入下面命令验证：

```Bash
python3.11 --version
python3.11 -m pip --version
```

应看到 `Python 3.11` 开头的版本和 pip 信息。若 `python3.11` 找不到，先确认 `.pkg` 安装成功并重开终端，保留错误后求助。

**版本说明**：3\.11\.9 提供传统图形安装器，后续 3\.11 安全修复版本的分发方式不同；它不是最新安全版本。上述路线用于课程本地练习，不作为生产部署版本建议。记录实际安装的完整版本；如果是用学校设备，有补丁要求时先找管理员采用符合要求的 3\.11\.x，不自行编译或绕过管理。

**检查点 A\-1**：版本与 pip 都有结果再继续。只弹出商店、只下载了安装文件，均不代表安装完成。

### 2\. Git

**Windows**：从 [Git 官方 Windows 下载页](https://git-scm.com/install/windows)选择适合自己系统的安装器。保留默认选项即可；默认编辑器选择不影响本讲，后续再调整。

**macOS**：先运行 `git --version`。如果没有可用 Git，按 [Git 官方 macOS 安装说明](https://git-scm.com/install/mac)使用 Apple 命令行工具。

```Bash
xcode-select --install
```

按系统弹窗完成安装，等待结束后重开终端。不需要安装完整 Xcode，也不必为本讲再安装一套软件包管理器。若系统提示已安装却仍不能运行 Git，保存完整提示后求助。

安装后先查看版本：

```Plaintext
git --version
```

这条命令在 Windows 和 macOS 都适用。个人身份留到第四节克隆成功后，只为课程仓库设置，避免覆盖其他项目的身份。Git 提交身份不是 GitHub 登录密码。首次配置可查 [Git 官方说明](https://git-scm.com/book/zh/v2/%E8%B5%B7%E6%AD%A5-%E5%88%9D%E6%AC%A1%E8%BF%90%E8%A1%8C-Git-%E5%89%8D%E7%9A%84%E9%85%8D%E7%BD%AE)。

**检查点 A\-2**：`git --version` 有真实版本输出。

### 3\. Visual Studio Code

安装 [VS Code 当前稳定版](https://code.visualstudio.com/Download)，再安装 Microsoft 发布的 Python 扩展：打开左侧“扩展”，搜索 `ms-python.python`，确认发布者为 Microsoft，再点击安装。课程不强制安装主题、AI 补全、Docker 或数据库扩展。

Windows 安装时勾选 **Add to PATH**，让终端能够找到 `code`。macOS 下载对应芯片版本，解压后将 Visual Studio Code 拖入“应用程序”，再打开；安装细节见 [官方 macOS 指南](https://code.visualstudio.com/docs/setup/mac)。安装后关闭并重新打开终端，再验证：

```PowerShell
code --version
```

如果该命令失败，打开 VS Code，使用 `帮助 → 关于` 抄录版本号，并在自检文档中注明“未加入 PATH”。macOS 可在命令面板运行 “Shell Command: Install 'code' command in PATH”。

打开 VS Code 后完成：

1. `终端 → 新建终端`

2. 确认终端能运行 `git --version`

3. 确认 Microsoft Python 扩展已安装

4. 后续打开课程工作区后，选择 `.venv` 中的 Python 解释器

官方资料：

- [VS Code 入门](https://code.visualstudio.com/docs/getstarted/getting-started)

- [VS Code Python 快速开始](https://code.visualstudio.com/docs/python/python-quick-start)

- [VS Code 集成终端](https://code.visualstudio.com/docs/terminal/getting-started)

**检查点 A\-3**：VS Code 能打开文件、Python 扩展已安装，版本已记录；`code` 命令暂不可用时可记录“关于”窗口的版本。

### 4\. Codex

本课程中的 Codex 是 AI 开发伙伴，不是 FlowERP 的一部分，也不是我们自行训练的大模型。它将帮助你读取仓库、提出计划、修改代码和运行命令，但你仍负责范围、权限、Diff 审查和最终验收。

本课程需准备两种入口：

1. **桌面入口**：从[官方桌面入口](https://learn.chatgpt.com/docs/app)安装应用并登录 ChatGPT 账号；第四节下载仓库后，再在第八节打开课程目录并实际对话。

2. **命令入口**：打开 [Codex CLI 官方安装页](https://learn.chatgpt.com/zh-Hans/docs/codex/cli)，选择自己操作系统对应的安装标签，按该页当前方法安装。现在先在终端运行 `codex --version`；第四节下载课程仓库后，第八节再进入该目录启动对话。

先选一种 CLI 安装方式即可，装桌面应用不等于终端已经能识别 `codex`。官方 CLI 提供独立安装入口；本课无需为了前端开发提前安装 Node\.js。若选择 npm 安装方式，才需要该方式的依赖。

可选：喜欢在编辑器内工作的学员可以安装 [Codex IDE 扩展](https://developers.openai.com/codex/ide)。它不能代替 Git、Python 和终端基础。

Codex 安装方式和版本更新较快，本课程不固定补丁版本。本节只要求：

```Plaintext
codex --version
```

现在只检查命令版本；做到第八节时，再确认**能实际发出只读问题并得到回复**。只打开登录页、只看到账号但无法发起对话，不算通过。优先使用 ChatGPT 账号登录；课程核心跟跑，不要求学员创建或提交 OpenAI API Key。账号档位、组织权限和额度以登录时官方界面为准，开营前必须自行验证。

**检查点 A\-4**：桌面入口可登录，终端显示 Codex CLI 版本。只读对话在第八节完成；订阅、权限和用量以你自己的账号显示为准，官方不提供。

## 三、不需要提前安装什么

今天需要的工具已经列在第二节。无需购买显卡或云服务器，也无需安装本地大模型、Docker、前端框架、外部 Agent 框架或独立数据库服务。若用官方独立安装入口安装 CLI，无需为了本课额外安装 Node\.js；自行选 npm 路线时才需要其依赖。

看到陌生工具名时，先回到我们资料中的步骤，不需要逐一安装。后续确有需要，会在对应任务中说明。



## 四、获取课程仓库

先选择一个路径简单、自己有写权限、**不会自动同步到 OneDrive / 坚果云 / 百度网盘**的目录。路径尽量只用英文和短名字。Windows 示例使用当前用户目录下的 `work`，无需 D 盘；`$env:USERPROFILE` 会自动展开为你的用户目录。如果该位置受学校管理或自动同步，改用管理员允许的本地目录并记下路径。

### Windows

```PowerShell
New-Item -ItemType Directory -Force "$env:USERPROFILE\work"
Set-Location "$env:USERPROFILE\work"
git clone https://github.com/congde/CodexFDE.git
Set-Location CodexFDE
git status
```

### macOS

```Bash
mkdir -p ~/work
cd ~/work
git clone https://github.com/congde/CodexFDE.git
cd CodexFDE
git status
```

上面每行依次是：建立存放课程的文件夹、进入它、下载带历史的课程仓库、进入仓库、检查状态。`git clone` 只在第一次获取时运行；如果提示同名目录已存在，先确认是不是已经克隆成功，不要删除现有文件。

**检查点 B\-1**：`git status` 能识别仓库，不出现 “not a git repository”。再用 VS Code 或文件管理器确认课程目录中有 `pyproject.toml`、`AGENTS.md` 和 `docs/courses/`。任一项缺失时，核对开营通知提供的仓库与资料版本，不继续猜路径。

记录此时的状态；后面新建自检文档会出现新文件，这是预期变化。下载 ZIP 只能作为网络受限时的临时方案；正式跟课需要 Git 历史、Diff 和后续分支能力。

克隆到的是**课程参考仓库**：里面包含工作台和 FlowERP 的参考代码。这只供你安装环境和阅读结构，**不是 L01 的起始基线**。L01 会在隔离工作区剥离本讲增量，要求你从头构造工作台；不要在这里把参考实现当成作业已完成。

### 克隆成功后：只设置本仓库的学习身份

确认 `git status` 已能识别课程仓库，再运行下面两条查询命令（两种系统通用）：

```Plaintext
git config user.name
git config user.email
```

没有输出可能只是尚未配置。若显示的身份不是你准备使用的学习身份，或显示为空，再执行下面的设置；已有合适身份可以跳过：

```Plaintext
git config --local user.name "你的学习署名"
git config --local user.email "你自己的邮箱"
git config user.name
git config user.email
```

将引号里的文字换成自己的信息。提交时姓名和邮箱会进入历史记录，使用自己愿意公开的学习身份或隐私邮箱，不复制老师的身份。这里的 `--local` 只影响当前课程仓库，不会改动其他仓库。



## 五、创建课程虚拟环境

虚拟环境的作用是，把课程使用的 Python 和电脑上其他项目隔离。不要把系统 Python 直接当课程运行环境。

**执行位置**：先在课程根目录确认能看到 `pyproject.toml`，再执行下面命令。如果刚重开终端，Windows 示例用 `Set-Location "$env:USERPROFILE\work\CodexFDE"`，macOS 示例用 `cd ~/work/CodexFDE`；路径按自己第四节实际选择填写。

### Windows 推荐方式

```PowerShell
py -3.11 -m venv .venv
.\.venv\Scripts\python.exe -m pip install --upgrade pip
.\.venv\Scripts\python.exe -m pip install -e .
.\.venv\Scripts\python.exe --version
```

第一行建立 `.venv`；第二行更新安装工具；第三行把当前课程项目安装到该环境，末尾的 `.` 表示当前目录；第四行查看版本。任一行失败时先停下排错，不继续执行后面的行。

本讲两种系统都直接使用 `.venv` 中的 Python，不需要另外“激活”。新开终端后只要回到课程目录，就能继续使用同一写法。

### macOS

```Bash
python3.11 -m venv .venv
.venv/bin/python -m pip install --upgrade pip
.venv/bin/python -m pip install -e .
.venv/bin/python --version
```

\.venv 创建成功后，`pip install -e .` 应正常结束，版本应为 Python 3\.11\.x。第七节将打印实际解释器路径，请确认它位于当前仓库的 `.venv` 中。`.venv` 已被 Git 忽略，不要提交它。

**检查点 B\-2**：安装成功、`.venv` 版本正确。已有 `.venv` 显示其他版本时，先停下并记录路径，不用重复创建命令覆盖它；请老师帮助重建合适环境。

## 六、打开课程工作区

在 VS Code 中打开：

```Plaintext
docs/courses/FlowERP-AI研发工作台.code-workspace
```

可以在 VS Code 选择“文件 → 从文件打开工作区”，定位上面的 `.code-workspace` 文件。若暂时找不到解释器入口，先打开一个 `.py` 文件，再打开命令面板（Windows：Ctrl\+Shift\+P；macOS：Command\+Shift\+P）。

确认 Python 解释器指向 `.venv`。如果没有自动识别，运行 “Python: Select Interpreter”，手动选择：

- Windows：`.venv\Scripts\python.exe`

- macOS：`.venv/bin/python`

两种系统的同学，都直接打开新终端，运行第七节命令，不点击工作区的课程任务按钮。那些按钮包含后续课次动作；任务已按操作系统选择本仓库的虚拟环境解释器。选择解释器后，仍以第七节实际打印的路径为准。打开一个 `.py` 文件只是为了查看解释器，不需要点击“运行 Python 文件”。

**检查点 B\-3**：左侧能找到课程资料，解释器指向本项目 `.venv`，新终端位于课程根目录。

## 七、完成第一次环境自检

先确认终端位于课程根目录。本节只检查版本、包导入、命令帮助和 Git 状态。启动服务、初始化业务数据、运行完整演示和全量测试留到正式课次；不要把 README 的“5 分钟跑起来”或工作区任务按钮当作本节操作清单。

### Windows

```PowerShell
.\.venv\Scripts\python.exe -X utf8 -c "import sys, sqlite3; print(sys.version); print(sys.executable); print('sqlite', sqlite3.sqlite_version)"
$LASTEXITCODE
.\.venv\Scripts\python.exe -X utf8 -c "import workbench, eval; print('imports-ok')"
$LASTEXITCODE
.\.venv\Scripts\python.exe -X utf8 -m workbench.cli --help
$LASTEXITCODE
git status --short
git diff --stat
code --version
codex --version
```

### macOS

```Bash
.venv/bin/python -X utf8 -c "import sys, sqlite3; print(sys.version); print(sys.executable); print('sqlite', sqlite3.sqlite_version)"
echo $?
.venv/bin/python -X utf8 -c "import workbench, eval; print('imports-ok')"
echo $?
.venv/bin/python -X utf8 -m workbench.cli --help
echo $?
git status --short
git diff --stat
code --version
codex --version
```

### 怎样保存，而不只说“成功了”

选中终端中对应的命令与输出，复制到自检记录。输出很长时，保留命令、关键结果、完整错误段和退出码；不要只抄最后一个绿色词。如果截图补充上下文，请先检查是否含账号或令牌。

以下是**格式示例，不是你的运行证据**：

```Plaintext
步骤：课程包导入
我执行的命令：在这里粘贴实际命令
实际输出：在这里粘贴实际输出
退出码：紧接该命令读取并填写
我的判断：这条结果证明了什么，尚未证明什么
```

### 怎样看这些结果

以上两组命令，只选择自己的系统执行。第一条显示 Python、实际解释器路径和 SQLite 版本；第二条能打印 `imports-ok`，说明工作台的 `workbench` 和 `eval` 包可以被找到；第三条显示可用命令帮助，不会启动完整产品。

FlowERP 客户产品已迁到独立仓库，`flowerp` 包使用该仓库自己的 `.venv`。本节不在 CodexFDE 环境中导入 `flowerp`，也不以 `imports-ok` 证明客户产品环境已就绪。若复制旧版自检命令后遇到 `No module named 'flowerp'`，改用上面的工作台自检命令；后续课次再按独立项目的准备步骤检查客户环境。

|看到什么|说明什么|下一步|
|---|---|---|
|路径包含当前仓库的 `.venv`|正在使用课程环境|继续检查导入|
|`imports-ok`|课程包可以导入|保存真实输出，不写“已开发工作台”|
|`usage:` 或命令帮助列表|命令入口能够加载|不必尝试帮助里所有子命令|
|`git status --short` 没输出|Git 没有列出待报告的改动|这可能是正常状态|
|`git diff --stat` 没输出|没有未暂存的已跟踪文本差异摘要|新文件仍可能存在，用 status 一起判断|
|`ModuleNotFoundError`|当前解释器找不到指定模块|核对目录、`.venv` 路径和安装步骤|

需要保存退出码时，在**刚执行完要检查的命令后**立即输入：Windows PowerShell 用 `$LASTEXITCODE`，macOS 终端用 `echo $?`。一般约定 0 表示该命令成功、非 0 表示失败；它只回答这一条命令，不能代表整个项目已经合格。不要先运行别的命令再记录旧命令的退出码。

**检查点 B\-4**：已保存解释器路径、`imports-ok`、命令帮助与 Git 状态。它们证明本机能加载课程代码；你的工作台构造将在 L01 另行完成。

## 八、第一次 Codex 练习：只读认识仓库

现在课程仓库已经下载完成。桌面入口选择第四节实际克隆得到的 `CodexFDE` 文件夹；CLI 入口先在终端进入同一个目录，再输入 `codex`，按界面完成登录。两种入口都确认能使用后，选择其中一种完成下面的对话即可，不必重复做两遍。

**启动 CLI 后，当前窗口会接收自然语言任务。**后续 Python、Git 自检请另开一个终端，不要把命令误发进 Codex 对话框。若出现权限询问，核对是否仍是本次只读检查；要求修改文件或扩大权限时，先停止并核对任务。

发送：

```Plaintext
请只读检查当前课程仓库，不修改任何文件，也不运行会改变状态的命令。
告诉我：
1. 仓库根目录路径；
2. Python 最低版本从哪个文件得出；
3. 课程大纲保存在哪个文件；
4. L01 开始时为什么不能假设个人工作台已经存在；
5. 仍有哪些结论你无法仅凭当前证据确认。
请给出可定位的文件路径，不要编造。
```

请至少人工打开两个被引用文件，核对 Codex 的回答。预习资料的重点不是“模型答对了”，而是第一次练习**让模型给证据、自己做核验**。若 Codex 引用了你打不开的路径，记入自检文档的未知项，不要编造文件内容。



## 九、用四个小练习检查自己是否准备好了

先做，再对照解释。可以查本文档；遇到不会的地方记录“需提示”，不用让 Codex 替你交一份全部正确的答案。这些练习不实现工作台，也不修改 ERP。

### 练习 1：我现在在哪里

在课程终端运行自己的系统对应命令：

```PowerShell
Get-Location
Get-ChildItem -Name
```

```Bash
pwd
ls
```

找到 `pyproject.toml` 和 `docs`。用 VS Code 打开 `pyproject.toml`，找到 `requires-python`，说出：仓库允许的最低版本与本班统一使用的版本有什么区别？

**对照解释**：当前仓库声明最低为 3\.10，课堂统一使用 3\.11\.x。能找到文件并说明来源，比复述 Codex 的回答更有用。如果看不到这两个名字，先检查自己是否在仓库根目录。

### 练习 2：先预测，再运行一个小结果

先在纸上回答：已有 20 件商品，其中 8 件留给已下单客户，还能再卖几件？然后选择一条命令运行：

```PowerShell
.\.venv\Scripts\python.exe -X utf8 -c "print(20 - 8)"
```

```Bash
.venv/bin/python -X utf8 -c "print(20 - 8)"
```

`-c` 表示执行后面引号内的小段程序，`print` 表示把结果显示出来。预期输出是 `12`，这是算术教学示例，不是实际库存数据，也不会写入业务账。

把 8 换成 5，先预测再运行。用自己的话解释：哪一部分是程序、哪一部分是输出？这条命令成功，能证明 FlowERP 库存功能已完成吗？

**对照解释**：输出应为 15；你检查的是一段算式，尚未检查真实订单、存储或重复操作，因此不能据此宣称产品已完成。

### 练习 3：失败不一定是电脑坏了

下面故意写了一个不成立的检查。先猜结果，再选择一条运行：

```PowerShell
.\.venv\Scripts\python.exe -X utf8 -c "available = 13; assert available == 12"
$LASTEXITCODE
```

```Bash
.venv/bin/python -X utf8 -c "available = 13; assert available == 12"
echo $?
```

`available` 是给结果起的名字，`=` 把右侧结果记到这个名字下；`assert` 检查一个说法，`==` 比较两边是否相等。这里故意把可用量写成 13，但检查要求仍是 12，所以预期出现 `AssertionError`，退出码非 0。分号把两条短语句放在同一行。

保存实际失败结果，再选择对应系统运行正确计算，**保持 ****`assert available == 12`**** 不变**：

```PowerShell
.\.venv\Scripts\python.exe -X utf8 -c "available = 20 - 8; assert available == 12"
$LASTEXITCODE
```

```Bash
.venv/bin/python -X utf8 -c "available = 20 - 8; assert available == 12"
echo $?
```

预期没有正文输出，退出码为 0。无需背诵这些语法，能指出“计算变了，检查要求没有变”即可。

**对照解释**：本练习修正了计算，保留同一检查要求，用来认识“先失败、修正后通过”。它仍只是算术示例，不是实际库存业务修复，也不能作为 L01 构造证据。如果连 `.venv` 的 Python 都找不到，那是环境问题，和断言失败需要分别处理。

### 练习 4：AI 的回答怎样变成我核验过的事实

回到第八节的 Codex 回答，挑出两条带文件来源的结论。亲自打开文件，分别记录“回答是什么、文件哪里支持、是否一致”。引用打不开、内容找不到或版本不同，都可以写“尚未确认”。

再给自己一道变化题：Codex 说“文件已修改”，`git diff` 没输出，是否一定没改？

**对照解释**：不能只凭这一条命令下结论。新建的未跟踪文件不会出现在普通 `git diff` 中，已暂存修改也要区别检查。先用 `git status --short` 查看状态；`??` 表示未跟踪新文件，`M` 表示修改。暂存区细节以后再学，今天先记住每条检查都有范围。

### 再试一道不看答案的小题（可选）

已有 30 件商品，留给已下单客户 9 件。先写出还能再卖几件，并尝试用前面学过的 `print` 显示计算结果，保存自己的命令和实际输出。

再分别判断两个现象：① 终端说找不到 `.venv` 中的 Python；② Python 已经执行，但出现 `AssertionError`。每种情况下，你会先检查什么、为什么？第二种情况需要同时核对计算与检查要求，不能为了通过而随意修改要求。

暂时不让 Codex 代答；可以查前面的语法解释。保留首次回答，再请老师反馈。没有收到反馈时可以提交自己的回答，之后补充。

**检查点 C**：四个练习都有实际动作与解释，两个引用文件已打开核验，变化题留下自己的回答。未完成项如实标记。

### 预习到哪里就够了

完成上述练习后，可以再读读 `AGENTS.md` 的项目目标与目录边界。提前准备一句自己的真实痛点，例如“我常让 AI 改东西，但下一次找不到它改了什么”，开营后带到 L01 的需求访谈。

Python 变量、列表、字典、函数和异常会随任务逐步学习，不要求在开课前全部掌握。[Python 官方教程](https://docs.python.org/3/tutorial/)明确面向已懂编程、刚接触 Python 的同学，适合作为查阅和有基础同学的延伸材料，零基础无需通读。

有经验的同学可以加做：解释 `.venv`、`pip install -e .` 与普通 `git diff` 各自的作用和限制；指出一条你会拒绝交给 AI 自动执行的操作及理由。预习不要求创建分支、提交代码或运行全量测试。



## 十、提交物

开始时已经保存记录的同学，继续使用同一份文件；只有临时记录的同学，现在转填模板，不必重跑成功步骤。

先打开 L00 空白提交模板。用“文件 → 另存为”将它保存到仓库根目录的 `lesson-00-submission` 文件夹，文件名为 `L00-环境自检.md`；文件夹不存在时先新建。不要覆盖模板文件。

Markdown 就是带简单标记的普通文本，直接填写即可。保存快捷键为 Windows 的 Ctrl\+S、macOS 的 Command\+S。若文件变成 `.md.txt`，在文件管理器中显示扩展名后改为 `.md`。

模板包含工具版本、原始命令记录、两条人工核验、四个练习、变化题和未决问题。不清楚的字段写“尚未确认”，不要借用他人的运行结果。绝对路径里包含个人信息时，公开提交可适度遮盖用户名，但保留能够说明 `.venv` 位置的部分。

**检查点 D**：确认文件已保存并能重新打开，开营后在指定渠道提交；截图可作补充，不能代替文本记录。不要提交密码、API Key、Cookie、客户数据或运行数据库。

## 十一、通过标准

用下面的清单逐项检查。完成时可以查文档或接受提示，但最后必须亲自操作、保存实际结果，并能解释关键现象。

- **准备完成**：环境可用，练习完成且能说明结果，引用已核验，记录齐全。

- **需要辅导**：工具能用，但部分操作或解释仍未完成；提交现有记录，请老师安排补练。

- **环境受阻**：安装、网络或登录阻止继续；提交错误记录，进入环境答疑。

三种状态都可以如实提交；“允许提交”不等于“全部通过”。是否需要先补学再进入 L01 动手，由老师依据具体卡点安排。

**检查清单：**

- Python 3\.11\.x 可以运行，且课程命令使用仓库内 `.venv`

- Git 可以运行并已设置自己的姓名与邮箱

- VS Code 能打开课程工作区并选择 `.venv`

- Codex 桌面入口可以登录，`codex --version` 可以运行，并且完成本讲只读对话

- 仓库成功克隆，`git status` 能识别

- Python 能导入 `sqlite3`，并打印 `imports-ok`

- `python -m workbench.cli --help` 可以运行

- 没有把 `serve`、`demo`、`course-status` 或全量测试当作预习通过证据

- 完成一次 Codex 只读提问，并人工核对至少两个文件

- 能找到目录、解释小程序输出，区分断言失败与环境错误，并完成第九节四个练习

- 自检文档放在 `lesson-00-submission/`，如实保留失败和未解决问题，没有提交敏感信息

预习不以“所有命令一次成功”为评分标准。真实失败、错误信息和解决过程都是准备证明；无法解决时，带着完整命令、输出和系统信息，参加开营前环境诊断，或者向讲师寻求帮助。

## 十二、常见问题：先找与你相同的现象

|现象|先检查什么|下一步|
|---|---|---|
|`pymanager` 找不到|是否装了 Python Install Manager，并重开 PowerShell|查第二节；不要把旧 `py` 启动器当管理器|
|`py -3.11` 或 `python3.11` 找不到|是否真正装了 3\.11，而不只是下载文件|保存安装结果，按对应系统路线核对|
|`.venv` 下的 Python 找不到|当前目录是否包含 `pyproject.toml` 和 `.venv`|回到课程根目录；尚未创建则完成第五节|
|`pip install -e .` 报“not a Python project”|命令最后的点和当前目录是否正确|在有 `pyproject.toml` 的目录重新尝试|
|包导入报 `ModuleNotFoundError`|刚才安装和现在运行是否用了同一个 `.venv`|对照实际解释器路径，不改成全局安装|
|克隆提示目录已存在|是否已经下载过；现有目录里有无作业|用文件管理器和 `git status` 检查，不删除重来|
|`not a git repository`|是否仍在 `work` 父目录，或下载的是 ZIP|进入真正的 `CodexFDE` 仓库；不运行 `git init` 冒充克隆|
|`code` 找不到|是否重开终端、VS Code 能否从图标打开|可先记“关于”版本，PATH 方法见第二节|
|`git` 或 `codex` 找不到|工具是否装好并重开终端|Windows 查 `where.exe git`／`where.exe codex`；macOS 查 `which git`／`which codex`|
|PowerShell 显示 `>>` 不执行|是否漏了引号、误复制了多行或提示符|按 Ctrl\+C 取消当前未完成输入，重新复制一条完整命令|
|macOS 显示 `quote>` 或 `dquote>`|引号是否不成对|按 Control\+C 取消未完成输入，重新复制完整一行|
|`AssertionError`|是否正在做练习 3 的故意失败步骤|按练习保存证据，不重装 Python|
|网络超时、登录失败或额度不足|保留明确错误，区分下载失败与账号限制|按第十三节求助，不反复重装或换不明账号|

电脑受管理、下载受限或需要离线材料时，联系讲师确认可用方案。这里不要求修改整机执行策略、关闭安全软件或使用不明安装源。

**中文显示异常**：`-X utf8` 只作用于 Python 的编码处理，不能修复所有工具与终端显示问题。保存原命令及现象，不改整机区域设置来碰运气。

**课程目录在网盘中**：优先使用不自动同步的本地目录，避免锁定和同步冲突。确需换目录时保留原作业与失败记录，再重新克隆，不直接删除原目录。中文路径不一定有错，短路径只是为了减少排错因素。



## 十三、卡住时，怎样把问题说清楚

你可以先请 Codex 帮忙理解，不急着允许它修改：

```Plaintext
我在完成 L00 环境准备。请先解释下面错误，不修改文件或系统设置。
先告诉我它更像目录、解释器、安装、网络还是账号问题。
只给一个最小检查步骤，说明在哪里运行、预期看到什么。
我返回实际结果后，再决定下一步。
我的系统：
当前目录：
实际命令：
实际输出：
```

仍无法解决时，把下面内容发到学习交流群里。先移除密码、令牌、Cookie 和私人业务数据，保留足够定位的命令与报错。

```Plaintext
我正在做：L00 第__节、第__步
操作系统及终端：
工具安装来源及版本：
当前目录（个人信息可适当遮盖）：
实际执行的命令：
实际输出和退出码：
原本预期看到：
我已经尝试过：
现在需要帮助确认：
```

如果 Codex 本身无法登录，直接把求助模板发给老师，不必先完成 AI 对话。可以标记“环境已通过，概念还需提示”，也可以标记“登录受阻，其他步骤已完成”。部分完成的真实记录，比一张无法解释的绿色截图，更能帮助老师安排支持。不要反复卸载工具、删除目录或扩大权限来碰运气。

## 十四、带着这三样东西进入 L01

1. 一份真实的环境自检文档，包含失败、解决过程和未决问题。

2. 一句自己的真实痛点，说明你希望工作台帮你记住或组织什么。

3. 一个你仍想问的问题，例如“我怎样知道 AI 真的运行了这条命令？”

下节课你将从这样的痛点出发，与 Codex 一起确定最小范围、形成第一份需求说明，再构造能记录项目、任务和命令证据的工作台。今天能说清自己做到了哪里，就已经为后续课程做好了准备。

## 官方安装资料

> 本轮查阅日期：2026\-09\-05。版本和安装方式以开营时官方页面为准。

- [Python：venv](https://docs.python.org/3/library/venv.html)

- [Git：Installing Git](https://git-scm.com/book/en/v2/Getting-Started-Installing-Git)

- [VS Code：Python environments](https://code.visualstudio.com/docs/python/environments)

- [OpenAI：Codex CLI](https://learn.chatgpt.com/docs/codex/cli)
