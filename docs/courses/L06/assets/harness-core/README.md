# 商品导入实验记录

2026-09-09 作者在临时 SQLite 数据库执行。教学输入与缺陷适配器明确标注，未修改生产服务。四份报告依次是错误汇总、正确阻断、修正导入、七行迁移。runs.json 保存独立进程退出码与输出。早期准备环境遗漏导致的外键失败仍保存在私有构建目录，不作为本轮业务反例。

参考 L05 回归来自 eval.cases.receiving_is_idempotent，不冒充个人 L05 用例或人员验收。所有图在 PPT 内可编辑。
