# 分开修复汇总与入口

在我提供的本讲候选中工作。允许文件仅 eval/l06_runner.py 和 flowerp/import_batch.py，其他文件、原检查、基线和报告保持原样。

先只修汇总器，按实际分项计算阻断与观察计数。保留异常和耗时，让九项合同通过，同时证明业务仍然 block。按操作手册保存新报告，不覆盖原失败。

然后修整批入口，将完整 CSV 一次性交给 validate_csv，再提交该任务。保留真实拒绝，不逐行提交、不跳过无效行、不吞异常。运行原六项与最终合同，再换七行输入。解释两处实际差异与仍未覆盖风险，不用常量伪造结果。
