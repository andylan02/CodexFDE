# 委托二：连接检查与候选

依据已确认 Spec：____，在允许文件：____ 中实现订单增量与项目 Hook。候选目录：____，项目解释器：____。通过已接受的控制工作台组织执行，保留任务与候选关联。

将待审查产物写入 hook_staging/hooks.json 和 hook_staging/quality_gate.py，保持活动 .codex 受保护。人工审查安装与真实触发另行留下同一任务的证据，不将暂存文件声称为已启用。

Hook 调用现有统一 Harness，新增业务断言登记在同一入口。处理输入、纯 JSON stdout、诊断 stderr、内部超时及异常；明确重入跳过意味着未再次验证。将实际测试目录与授权候选对应，避免测试控制仓库。

先给出最小改动和可执行复验入口，再实施授权范围内的变更。针对个人实现覆盖正常、失败、重入和故障分支；记录注入缺陷的来源。报告实际 Diff、业务状态、运行结果及尚缺的真实宿主证据。不降低质量规则，也不把自身完成说明当成验收。

将两行金额、非法请求无残留和迁移金额接到同一 Harness；保持 L06 既有检查。教师参考函数见 ../examples/order_contract_checks.py，个人测试须调用自己的候选。手工事件配合真实子进程仍不是真实宿主事件，输出中分别标识。

本次诊断结论与证据：____。先明确工作台改进前后行为。验证触发时保持业务缺陷和断言不变；验证业务修复时保留原断言。教学缺陷只在授权隔离副本中使用，最终候选另行复验。
