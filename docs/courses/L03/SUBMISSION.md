# L03 提交模板

将本模板复制到自己的 `lesson-03-submission/`。空项保留“未完成”或“未验证”；示例不能充当真实记录。

## 1. 初始判断与起点

- 学习仓库与实际解析模块：
- 使用自己的起始实现，还是课程参考终态：
- 我在调用 Codex 前识别出的需求缺口：
- 来源、课堂假设与未知项：

## 2. 我的需求决定

| 待决定问题 | 选择及理由 | 来源／实际决定人 | 会改变哪条验收 |
|---|---|---|---|
| 使用目的与库存口径 | | | |
| 字段、排序与空集 | | | |
| 失败及不变状态 | | | |

- 原始 Spec：
- 修订后 Spec：
- 我采纳、修订或拒绝的模型建议及理由（没有拒绝可如实写明）：
- 合同检查点（`contract_ready` / `draft`）及证据：

## 2.1 架构与需求交接

- 我的 Spec 路径与版本、调查及方案建议所在的决定记录：
- 对照架构图，工作台内部是什么，Codex 与 FlowERP 分别负责什么：
- 业务范围与允许修改文件为什么不是一回事：
- 调研只读权限与产品非目标的区别：
- 更改业务口径后，哪些确认需要重新取得：
- 哪些能力由 L03 实现，哪些留给 L04 及后续讲次：

| 业务要求 | Spec 条目 | 具体数据、操作、结果与失败后状态检查 |
|---|---|---|
| 可用量口径 | | |
| 空库存 | | |
| 写出失败 | | |

- 结构合法但语义错误的反例、解析结果与我的评审：
- 我认为工作台相较直接对话新增了什么可检查的约束，证据在哪里：

## 2.2 模板复用

- 模板路径与版本：
- 库存 Spec 与采购草稿是否来自同一模板，证据在哪里：
- 已删除的库存专属规则，以及采购稿尚未确认的业务条件：
- 当前合同的实际确认人、日期与状态（课堂角色练习须标明）：

## 3. 解析器建设

- 接口和允许修改文件：
- 实际测试模块及被测试模块：
- 实现前输出与原因（已有能力通过时如实说明）：
- 实际代码差异：
- 同一测试实现后输出、数量与退出码：

## 3.1 工作台入口接入

- CLI → load_spec → 个人解析函数的文件路径与接入 Diff：
- 同一工作台入口读取库存与采购草稿的命令、六字段核对和退出码：
- 缺项副本的报错与退出码、运行前后输入未改变的依据：
- 合同闸门检查点（`gateway_connected` / `parser_only` / `能力待接入`）及证据：

## 4. 拒绝行为与语义审查

| 输入变化 | 预测 | 实际命令、结果与退出码 | 失败后状态 |
|---|---|---|---|
| 完整输入 | 接受，字段与原文一致 | | |
| 缺少必要章节 | 明确拒绝 | | |
| 另一种结构错误 | | | |
| 结构合法但库存口径错误 | 结构可能通过，人工用反例退回 | | |

语义评审不是程序运行，不虚构它的退出码。

## 5. 反馈、修订与迁移

- 同伴实际复述与反例（无同伴则注明）：
- 我修改了什么，为什么；未采纳反馈的理由：
- 第二次核对人和日期：
- 换成采购申请导出，需要重新决定的内容：
- 目前证据仍不能证明什么：
- 最终交接检查点（`handoff_ready` / `review_required`）及确认版路径、版本、SHA256：

本讲交接 L04 的是库存导出合同和解析能力，不能填写“库存导出功能已交付”。

## 配套 skill 采用记录

- 本次使用的 skill 路径与版本：
- 练习输入、我的预期与 Codex 实际响应：
- 哪项判断有依据，哪项仍错误或未证明：
- 我修订了哪条流程及复验结果：
- 后续任务是否已采用；尚未发生写“未验证复用”：
